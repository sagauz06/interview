<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
<meta charset="BIG5">
<title>Create new Card</title>
</head>
<body>
	<div align="center">
		<h1>Create New Card</h1>

		<form action="#" th:action="@{/save}" th:object="${yoyocard}"
			method="post">
			<table border="0" cellpadding="10">
				<tr>
					<td>Card ID:</td>
					<td><input type="text" th:field="*{id}" /></td>
				</tr>
				<tr>
					<td>Card Name:</td>
					<td><input type="text" th:field="*{name}" /><span
						class="validationError" th:if="${#fields.hasErrors('name')}"
						th:errors="*{name}">name Error</span></td>
				</tr>
				<tr>
					<td>Card Money:</td>
					<td><input type="text" th:field="*{money}" /> <span
						class="validationError" th:if="${#fields.hasErrors('money')}"
						th:errors="*{money}">money Error</span></td>
				</tr>
				<tr>
					<td colspan="2"><button type="submit">Save</button></td>
				</tr>
			</table>
		</form>
	</div>
</body>
</html>